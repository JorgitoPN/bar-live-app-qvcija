
# Redirect rules for Barlive

# Auth routes - Password reset flow
/auth/reset-password-web /auth/reset-password-web 200
/auth/recuperar-password-v6 /auth/recuperar-password-v6 200
/auth/restablecer-password /auth/restablecer-password 200
/auth/recuperar-password /auth/recuperar-password 200

# Legacy auth routes
/auth/login /auth/login 200
/auth/registro /auth/registro 200
/auth/callback /auth/callback 200

# Email confirmation
/email-confirmed /auth/email-confirmed 200

# Admin routes
/admin/* /admin/:splat 200

# Social routes
/social/* /social/:splat 200

# Profile routes
/perfil/* /perfil/:splat 200

# Legal routes
/legal/* /legal/:splat 200

# API routes (if any)
/api/* /api/:splat 200

# Fallback to index for SPA
/* /index.html 200
