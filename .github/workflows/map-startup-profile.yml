name: BarLive map cold-start profile

on:
  push:
    branches:
      - gh-pages
    paths:
      - '.github/workflows/map-startup-profile.yml'
      - '.github/scripts/map-startup-profile.cjs'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  profile:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout profiler
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          sparse-checkout: |
            .github/scripts/map-startup-profile.cjs
          sparse-checkout-cone-mode: false
      - name: Install Playwright Chromium
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/barlive-startup-profile
          cd /tmp/barlive-startup-profile
          npm init -y >/dev/null 2>&1
          npm install playwright@1.57.0 --no-audit --no-fund
          npx playwright install --with-deps chromium
          cp "$GITHUB_WORKSPACE/.github/scripts/map-startup-profile.cjs" .
      - name: Profile production cold start
        shell: bash
        run: |
          set -euo pipefail
          cd /tmp/barlive-startup-profile
          node map-startup-profile.cjs
