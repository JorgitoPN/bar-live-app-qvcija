name: BarLive hybrid map smoke

on:
  push:
    branches:
      - gh-pages
    paths:
      - '.github/workflows/map-hybrid-smoke.yml'
      - '.github/scripts/map-hybrid-smoke.cjs'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hybrid-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout hybrid smoke
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          sparse-checkout: |
            .github/scripts/map-hybrid-smoke.cjs
          sparse-checkout-cone-mode: false

      - name: Install Playwright Chromium
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/barlive-hybrid-smoke
          cd /tmp/barlive-hybrid-smoke
          npm init -y >/dev/null 2>&1
          npm install playwright@1.57.0 --no-audit --no-fund
          npx playwright install --with-deps chromium
          cp "$GITHUB_WORKSPACE/.github/scripts/map-hybrid-smoke.cjs" .

      - name: Test production hybrid renderer
        shell: bash
        run: |
          set -euo pipefail
          cd /tmp/barlive-hybrid-smoke
          node map-hybrid-smoke.cjs
