name: Apply BarLive map viewport race fix

on:
  push:
    branches:
      - gh-pages
    paths:
      - '.github/workflows/apply-map-race-fix.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: barlive-map-race-fix
  cancel-in-progress: false

jobs:
  patch-production-bundle:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout gh-pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          fetch-depth: 0

      - name: Patch canonical map bundle
        run: node .github/scripts/apply-map-race-fix.js

      - name: Commit production race guard
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "No changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(web-map): guard viewport loads from state refresh"
          git push origin HEAD:gh-pages
