
╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║   🔧 GUÍA VISUAL: Fix R8 Error - Expo Kotlin Runtime         ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────┐
│ PASO 1: Abrir el archivo ProGuard                            │
└───────────────────────────────────────────────────────────────┘

    📁 android/
        📁 app/
            📄 proguard-rules.pro  ← ABRIR ESTE ARCHIVO


┌───────────────────────────────────────────────────────────────┐
│ PASO 2: Agregar reglas al FINAL del archivo                  │
└───────────────────────────────────────────────────────────────┘

    proguard-rules.pro
    ┌─────────────────────────────────────────────────────────┐
    │ # react-native-reanimated                               │
    │ -keep class com.swmansion.reanimated.** { *; }          │
    │                                                          │
    │ # Add any project specific keep options here:           │
    │                                                          │
    │ ← PEGAR LAS NUEVAS REGLAS AQUÍ ↓                        │
    │                                                          │
    │ # Expo Modules - Keep all classes and members           │
    │ -keep class expo.modules.** { *; }                      │
    │ -keepclassmembers class expo.modules.** { *; }          │
    │ -dontwarn expo.modules.**                               │
    │                                                          │
    │ # Expo Kotlin Runtime                                   │
    │ -keep class expo.modules.kotlin.** { *; }               │
    │ -keepclassmembers class expo.modules.kotlin.** { *; }   │
    │ -dontwarn expo.modules.kotlin.**                        │
    │                                                          │
    │ # Expo Media Library                                    │
    │ -keep class expo.modules.medialibrary.** { *; }         │
    │ -keepclassmembers class expo.modules.medialibrary.** {  │
    │                                                          │
    │ # Kotlin Standard Library                               │
    │ -keep class kotlin.** { *; }                            │
    │ -keep class kotlin.Metadata { *; }                      │
    │ -dontwarn kotlin.**                                     │
    │                                                          │
    │ # Kotlin Reflection                                     │
    │ -keepclassmembers class **$WhenMappings { <fields>; }   │
    │ -keepclassmembers class kotlin.Metadata { public <...   │
    │                                                          │
    │ # Kotlin Coroutines                                     │
    │ -keepnames class kotlinx.coroutines.internal.MainDis... │
    │ -keepnames class kotlinx.coroutines.CoroutineExcepti... │
    │ -keepclassmembers class kotlinx.** { volatile <fields>; │
    └─────────────────────────────────────────────────────────┘


┌───────────────────────────────────────────────────────────────┐
│ PASO 3: Limpieza Profunda (CRÍTICO)                          │
└───────────────────────────────────────────────────────────────┘

    📁 android/
        📁 .gradle/          ← ELIMINAR ESTA CARPETA
        📁 app/
            📁 build/        ← ELIMINAR ESTA CARPETA
            📄 proguard-rules.pro

    Luego ejecutar:
    
    $ cd android
    $ ./gradlew clean
    $ cd ..


┌───────────────────────────────────────────────────────────────┐
│ PASO 4: Compilar para Release                                │
└───────────────────────────────────────────────────────────────┘

    Ejecutar tu comando de build para Release
    
    ✅ El build debería completarse exitosamente
    ✅ No más error "Missing class expo.modules.kotlin.runtime.Runtime"


┌───────────────────────────────────────────────────────────────┐
│ PASO 5: Commit y Push                                        │
└───────────────────────────────────────────────────────────────┘

    $ git add android/app/proguard-rules.pro
    $ git commit -m "fix: add comprehensive ProGuard keep rules for Expo and Kotlin runtime"
    $ git push


╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║   ✅ ¡LISTO! El error de R8 está resuelto                     ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝


┌───────────────────────────────────────────────────────────────┐
│ 🎯 POR QUÉ FUNCIONA                                           │
└───────────────────────────────────────────────────────────────┘

    ANTES:
    ┌─────────────────────────────────────────────────────────┐
    │ R8: "No veo referencias a expo.modules.kotlin.runtime"  │
    │ R8: "Voy a eliminar estas clases para ahorrar espacio" │
    │ R8: "¡Eliminado!"                                       │
    │                                                          │
    │ En runtime: "¿Dónde está la clase?"                     │
    │ ❌ ClassNotFoundException                               │
    └─────────────────────────────────────────────────────────┘

    DESPUÉS:
    ┌─────────────────────────────────────────────────────────┐
    │ ProGuard: "R8, mantén estas clases"                     │
    │ R8: "Entendido, las mantengo"                           │
    │ R8: "✅ Clases preservadas"                              │
    │                                                          │
    │ En runtime: "Clase encontrada"                          │
    │ ✅ Todo funciona correctamente                           │
    └─────────────────────────────────────────────────────────┘


┌───────────────────────────────────────────────────────────────┐
│ 📊 IMPACTO                                                    │
└───────────────────────────────────────────────────────────────┘

    Tamaño APK:     +50-100 KB (aceptable)
    Rendimiento:    Sin cambios
    Seguridad:      Sin cambios
    Funcionalidad:  ✅ Todo funciona


┌───────────────────────────────────────────────────────────────┐
│ 🔍 SI EL ERROR PERSISTE                                       │
└───────────────────────────────────────────────────────────────┘

    1. Verifica que las reglas se agregaron correctamente
    2. Asegúrate de haber eliminado las carpetas de caché
    3. Ejecuta ./gradlew clean de nuevo
    4. Intenta con: ./gradlew assembleRelease --no-daemon


═══════════════════════════════════════════════════════════════
