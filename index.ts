
// Initialize Natively console log capture before anything else
// This is safe to import - it only enhances console output in dev mode
import './utils/errorLogger';

// Start the Expo Router app
import 'expo-router/entry';
