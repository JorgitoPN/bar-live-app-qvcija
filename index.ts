// ✅ CRITICAL FIX v22.0: Temporarily disabled error logger to fix Metro bundler issues
// The error logger was causing "getDevServer is not a function" errors
// TODO: Re-enable after fixing Metro bundler compatibility

import 'expo-router/entry';
