# --- PROTECCIÓN ANTI-BLOQUEO (NATIVELY/GITHUB) ---
node_modules/
.natively/
chat_history.json
*.log
*.tmp
temp/
cache/
.expo/
.git/

# --- ARCHIVOS DE EXPO Y COMPILACIÓN ---
dist/
web-build/
expo-env.d.ts
.metro-health-check*
*.tsbuildinfo

# --- SEGURIDAD Y LLAVES (MUY IMPORTANTE) ---
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem
.env*.local

# --- SISTEMA Y BASURA ---
.DS_Store
*.orig.*

# --- GESTORES DE PAQUETES ---
# Bloqueamos los locks porque a veces Natively se confunde con versiones locales
pnpm-lock.yaml
package-lock.json
npm-debug.*
yarn-debug.*
yarn-error.*

# --- CARPETAS NATIVAS ---
# Expo las genera en la nube, no las necesitamos en GitHub
android/
ios/

# --- DESPLIEGUE ---
netlify.toml
.netlify/
.natively/expo_server.log
